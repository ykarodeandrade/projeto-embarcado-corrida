#include <asf.h>

// precisa definir as funcoes no acelerador.h

short get_acelerador(uint32_t afec, uint32_t channel) {
	// fazer a leitura do analogico aqui e retornar um valor correspondente de 0 a 255
	return 0;
}
